services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      APP_HOST: 0.0.0.0
      PORT: 5000
      FLASK_DEBUG: "0"
      SECRET_KEY: ${SECRET_KEY:-change-this-secret}
    volumes:
      - ./instance:/app/instance
